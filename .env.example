# ==============================================# Flask

# 环境变量配置文件示例FLASK_ENV=development

# ==============================================SECRET_KEY=replace-with-a-random-secret

# 说明：

# 1. 复制此文件为 .env 用于本地开发# Database

# 2. 生产环境请修改所有密钥和密码# 开发期：如用本地 PG，请自行改为 localhost; 如用 compose 中的服务名，容器内为 db

# ==============================================DATABASE_URL=postgresql+psycopg2://mlogger:mlogger@db:5432/mlogger



# ============ 应用环境 ============# Uploads

# development, production, testing# 容器内推荐 /data/uploads；本地直跑可改为绝对路径

FLASK_ENV=developmentUPLOAD_FOLDER_BASE=/data/uploads



# ============ 数据库配置 ============# Logging

# Docker Compose 使用LOG_LEVEL=info

DB_NAME=mlogger

DB_USER=mlogger# Optional: Monitoring

DB_PASSWORD=mlogger123SENTRY_DSN=

SENTRY_TRACES_SAMPLE_RATE=0.0

# 本地开发使用（SQLite）

# DATABASE_URL=sqlite:///instance/dev.db# Optional: Redis（第 3 步会用到）

REDIS_URL=redis://redis:6379/0

# 生产环境使用（PostgreSQL）
# DATABASE_URL=postgresql+psycopg2://mlogger:mlogger123@localhost:5432/mlogger

# ============ 安全密钥 ============
# 生产环境必须修改为强随机字符串！
# 生成方法：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production

# ============ CORS 配置 ============
# 允许的前端域名，多个用逗号分隔
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:80

# ============ 上传配置 ============
# 本地开发
UPLOAD_FOLDER=uploads
# Docker 容器内
# UPLOAD_FOLDER=/app/uploads

# ============ 前端配置 ============
# 后端 API 地址（前端构建时使用）
VITE_API_BASE_URL=http://localhost:5000

# ============ 日志配置 ============
LOG_LEVEL=INFO

# ============ Sentry 监控（可选）============
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.1

# ============ Redis 缓存（可选）============
# REDIS_URL=redis://localhost:6379/0
